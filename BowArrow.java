public class BowArrow extends Weapon
{
    public BowArrow(Handler handler, int x, int y, int width, int height)
    {
        super(handler, x, y, width, height, Weapon.bowDamage, Weapon.slayerID);

    }
}
